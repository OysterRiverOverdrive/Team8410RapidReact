This is where we will have the intelligence type of classes which is reposnable to get values
from the sensor and determine if the sensor is working well. This class will set the "constants" 
with sensor values 